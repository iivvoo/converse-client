import { timestampFormat } from '../../../helpers/timestamp-format';
import { module, test } from 'qunit';

module('Unit | Helper | timestamp format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timestampFormat([42]);
  assert.ok(result);
});
